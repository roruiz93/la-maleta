# TODO: Fix npm run dev (Vite not recognized)

## Steps from approved plan (updated with fixes needed):\n- [ ] Step 3: Fix Vite errors (missing `initDestinos` export and HTML deps scanning)\n  - Read relevant HTML/JS files\n  - Add missing exports/functions\n  - Update vite.config.js if needed for HTML deps
- [x] Step 1: Run `npm install` to install dependencies (Vite, Firebase, etc.) ✓ (completed, audited 168 packages, some moderate vulns - optional to audit fix later)
- [x] Step 2: Run `npm run dev` to start Vite dev server ✓ (Vite server started at http://localhost:5173/, but runtime errors in HTML imports - need fixes)
- [ ] Step 3: Verify server starts (http://localhost:5173)

Updated after each completed step.

